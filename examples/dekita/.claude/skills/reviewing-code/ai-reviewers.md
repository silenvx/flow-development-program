# AIレビュアー

GitHub上のAIレビュアー一覧と特性。

## AIレビュー一覧

**重要**: AIレビュアーは追加・削除されることがある。以下は代表的なもの。

| レビュアー | ログイン名 | トリガー | 特記事項 |
| ---------- | ---------- | -------- | -------- |
| GitHub Copilot | `copilot-pull-request-reviewer[bot]` | 自動（PR ready時） | - |
| Codex Cloud | `chatgpt-codex-connector[bot]` | `@codex review` コメント | - |
| Gemini Code Assist | `gemini-code-assist[bot]` | リポジトリ設定による | - |
| Cubic Dev AI | `cubic-dev-ai[bot]` | リポジトリ設定による | - |
| CodeRabbit | `coderabbitai[bot]` | 自動（PR作成時） | コメント編集、レートリミット |
| Qodo Code Review | `qodo-code-review[bot]` | 自動（PR作成時） | コンプライアンスチェック |
| Greptile | `greptile-apps[bot]` | 自動（PR作成時） | Overview、コード提案 |
| Codex CLI | （ローカル実行） | `codex review --base main` | - |

### CodeRabbitの特性（Issue #3170）

CodeRabbitは他のAIレビュアーと異なる動作をする:

1. **コメント編集**: 新規コメント投稿ではなく、既存コメントを編集して更新する
   - `created_at` と `updated_at` を比較することで編集を検出可能
2. **レートリミット**: 時間あたりのレビュー数に制限あり
   - 制限時、待機時間が表示される（例: `wait **0 minutes and 41 seconds**`）
   - `@coderabbitai review` コメントでリトライ可能
3. **他AIレビュアー検出**: 他のAIレビュアーがいると、重複回避でレビューを省略することがある

### Qodo Code Reviewの特性（Issue #3274）

Qodoはコンプライアンスチェックを行うAIレビュアー:

1. **ステータス**: 🟢 Compliant / 🔴 Not Compliant / ⚪ Requires Human Verification
2. **コミット単位評価**: 「Compliance updated until commit xxx」で評価対象を表示
3. **マージブロック**: 🔴 Not Compliant があるとマージ不可

#### コンプライアンス再評価

Qodoが古いコミットを参照している場合:

```bash
# 最新コミットと比較し、差分があれば再評価を依頼
git log -1 --format=%H  # 最新コミット確認
gh pr comment {PR} --body "/compliance"  # 再評価依頼
```

**背景**: PR #3229でQodoが古いコミットを見ていたためマージがブロックされた。

#### Q&A形式のやり取り（Issue #3908）

Qodoはコメントに対して「Question → Answer」形式で応答することがある:

1. **Question（トリガー）**: Claude Codeがqodoの指摘に回答
2. **Answer（Qodoの返答）**: Qodoが追加の提案を返す

**例**: PR #3904 でブランチ名パターンについて回答したところ、Qodoが `.pr_agent.toml` の設定案を提案

**対応方法**:

| Qodoの提案内容 | 対応 |
| -------------- | ---- |
| `.pr_agent.toml` 設定案 | 有用ならIssue化して検討 |
| 仕様変更の提案 | プロジェクト方針と照らして判断 |
| ドキュメント参照 | 参考情報として記録 |

**注意**: Qodoの「Answer」は提案であり、必ずしも対応が必要ではない。プロジェクトの既存仕様を優先する。

### 新しいAIレビュアーへの対応

PRレビューで未知のbotからコメントがあった場合:

1. ログイン名を確認: `gh api /repos/{owner}/{repo}/pulls/{PR}/reviews --jq '.[].user.login' | sort -u`
2. 対応の必要性を判断（レビューコメントか、自動通知か）
3. レビューコメントなら通常通り対応
4. 必要に応じて `.claude/hooks/lib/constants.ts` に追加:
   - `COPILOT_CODEX_IDENTIFIERS`: 待機対象のレビュアー（Copilot/Codex/Cubic等）
   - `AI_REVIEWER_IDENTIFIERS`: 全AIレビュアー検出用（上記 + Gemini）

## レビュー確認方法

**重要**: PRには**3種類**のコメントAPIがある。全て確認すること。

| API | 内容 | 例 |
|-----|------|-----|
| `pulls/{PR}/comments` | コード行コメント | インライン指摘 |
| `pulls/{PR}/reviews` | PRレビュー全体 | "No issues found"、概要 |
| `issues/{PR}/comments` | **会話コメント** | qodo Suggestions、greptile Overview |

```bash
# 1. コード行コメント（インライン指摘）
gh api /repos/{owner}/{repo}/pulls/{PR}/comments --jq '.[] | {user: .user.login, path, line, body}'

# 2. PRレビュー全体（"No issues found" 等）
gh api /repos/{owner}/{repo}/pulls/{PR}/reviews --jq '.[] | {user: .user.login, state, body}'

# 3. 会話コメント（qodo Suggestions、greptile Overview 等）
gh api /repos/{owner}/{repo}/issues/{PR}/comments --jq '.[] | {user: .user.login, body}'

# レビュー進行中確認（Copilot/Codexがいたらマージ待機）
gh api /repos/{owner}/{repo}/pulls/{PR} --jq '.requested_reviewers[].login'
```

**重要**: `requested_reviewers` に `Copilot` や `codex` がいたらレビュー進行中。マージを待つこと。

> **注**: `{owner}`, `{repo}`, `{PR}` は実際の値に置き換え。`gh api` は `{owner}/{repo}` を自動解決するため、そのまま使用可能。

## Codex CLIローカルレビュー

PRプッシュ前に実行:

```bash
# mainとの差分をレビュー
codex review --base main

# 未コミット変更をレビュー
codex review --uncommitted

# 特定観点でレビュー
codex review --base main --instructions "セキュリティを重点チェック"
```

### 設計品質レビュー（推奨）

結合度・凝集度・SRPなどの設計原則を重点的にチェック:

```bash
# 設計品質レビュー（結合度・凝集度・SRP含む）
.claude/scripts/codex_design_review.sh

# セキュリティ重点レビュー
.claude/scripts/codex_design_review.sh --security

# 結合度重点レビュー
.claude/scripts/codex_design_review.sh --coupling

# 凝集度重点レビュー
.claude/scripts/codex_design_review.sh --cohesion

# 未コミット変更のレビュー
.claude/scripts/codex_design_review.sh --uncommitted
```

プロンプトは `.claude/docs/design-review-prompt.txt` で定義。
